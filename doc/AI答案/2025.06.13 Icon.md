

1. 内置 Icon 有哪些?
2. 如何扩展 Icon?

Rocket.Chat 支持通过 Fuselage 的 <Icon /> 组件注册自定义 SVG icon。
一般做法是：
在前端项目里引入自定义 SVG 组件
用 Fuselage 的 addIcon 方法注册，例如：
```
import { addIcon } from '@rocket.chat/fuselage';
import MyCustomIcon from './MyCustomIcon.svg';

addIcon('my-custom-icon', MyCustomIcon);
```